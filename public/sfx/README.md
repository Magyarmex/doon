# SFX Assets

This directory is reserved for procedural or externally hosted sound references. Rifle shots are now synthesized at runtime to avoid storing binary assets in the repository. The audio library still applies a 50% volume taper to the latter half of each clip.
